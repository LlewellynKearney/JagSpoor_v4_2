# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which can carry live API keys / credentials (e.g. GEMINI_API_KEY in
# launch.json). The live `.vscode/launch.json` MUST stay untracked. Only the
# sanitized template `.vscode/launch.json.example` is committed so new
# environment setups have a reference config.
#
# Ignore the live launch configuration and any other local VS Code files.
# NOTE: `.vscode/*` is used as the operative ignore rather than a bare
# `.vscode/` because Git will not descend into a directory ignored with a
# trailing slash, which would make the `!launch.json.example` negation below
# impossible. `.vscode/*` ignores the directory's contents while still
# letting Git descend so the template can be re-included.
.vscode/*
.vscode/launch.json
# Keep the sanitized template (no live keys) tracked for new env setups.
!.vscode/launch.json.example

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Flutter SDK (temporary for analysis)
flutter/
flutter.tar.xz

# Node / Firebase Cloud Functions
node_modules/
functions/node_modules/
functions/lib/
functions/.env
functions/.env.local
functions/*.local
